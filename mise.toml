[tools]
rust = "latest"

[tasks]
check = "cargo check"
build = "cargo build"
develop = "uv run maturin develop --release --uv"
test = "uv run python tests/test_basic.py && uv run python tests/test_shapes.py"
bench = "uv run python benchmarks/bench_matmul.py"
